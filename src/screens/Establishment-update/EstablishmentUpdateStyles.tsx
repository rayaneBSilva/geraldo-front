import {StyleSheet} from "react-native";

export const establishmentStyles = StyleSheet.create({
    backgroundImage: {
        flex: 1,
        width: "100%",
        height: "100%",
        resizeMode: "cover",
        marginTop: -30,
      },
});